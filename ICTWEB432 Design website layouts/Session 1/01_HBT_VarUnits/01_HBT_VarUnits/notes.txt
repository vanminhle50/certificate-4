[B] RESPONSIVE CSS PROPERTIES

We need to develop our website to be flexible to adjust to different devices and browser sizes.  There are a number of methods that we can use to achieve this.

[1.3] ADJUSTING HBT FOR RESPONSIVE UNITS

The base HBT web project was adjusted in the following order:

  - (a) ROOT VARIABLES
    - Add in root variables JUST for colours for timebeing
    - Select colours declared & replace with root var names (use CTRL + D selecting)
  
  - (b) RESPONSIVE UNITS (examples)
    - Replace font-sizes, line-heights with em/rem
    - Replace SOME margins with rem
    - Some page-wide sizes with vh
    - Replace SOME heights/widths with % where applicable (note line 195 especially!)

  - (c) OVERALL ALTERATIONS AHEAD OF BIGGER CHANGES
    - Add new image assets & (about to remove some OLD images)
    - New page of `about.html` with boilerplate
    - Custom web font of RALEWAY
    - Added CSS Utilities:
      -> .text-highlight
      -> .text-standard & .text-tiny
      -> .wide-container